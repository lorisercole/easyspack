# only needed to make Spack read a custom database generated by ebspack
# (not needed if you just use external packages defined in packages.yaml)
upstreams:
  eessi:
    install_tree: /home/lercole/eessi/spack/upstreams/eessi
